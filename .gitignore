# Build output (regenerated by CI on every push — never commit this)
build/
build*/
**/build/

# CMake cache/config leftovers if a build was ever configured in-place
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
CTestTestfile.cmake
compile_commands.json

# Ninja
*.ninja_log
*.ninja_deps
build.ninja

# VS Code / editor cruft
.vscode/
.clangd
.cache/

# STM32CubeMX/CubeIDE workspace junk (keep the .ioc file itself, just not generated logs)
*.log

# OS junk
.DS_Store
Thumbs.db
